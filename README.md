# docker-dynamo-local

![DynamoDB Logo](http://upload.wikimedia.org/wikipedia/commons/1/11/DynamoDB.svg)

Dockerized DynamoDB Local.

## Usage

```
sudo docker run -d -p 8000:8000 --name dynamodb kcyeu/dynamodb-local
```
